# using functions on Objects
# functions that are used with an objects are called methods
# the state of the object is found through the attribute of the object
# methods are the name of functions that belong to an object
# calling a function in a method
# ? we use the dot notation object.attribute()